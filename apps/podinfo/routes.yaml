apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: podinfo
spec:
  hostnames:
  - poshcode.com
  parentRefs:
  - namespace: cilium
    name: damascus
    sectionName: https-poshcode
  rules:
  - backendRefs:
    - name: podinfo
      port: 9898
    matches:
    - path:
        type: PathPrefix
        value: /


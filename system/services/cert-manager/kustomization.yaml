resources:
  - namespace.yaml
  - repository.yaml
  - release.yaml

configMapGenerator:
  - name: cert-manager-values
    namespace: flux-system
    files:
      - values.yaml=cert-manager-v1.14.3-values.yaml

configurations:
  - valuesfrom.yaml
# Azure Authentication Method
- target:
    kind: Deployment
    name: "(kustomize-controller)"
  patch: |
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: controller
    spec:
      containers:
      - name: manager
        env:
        - name: AZURE_AUTH_METHOD
          value: msi
